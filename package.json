{
  "name": "code-a-thon",
  "version": "1.0.0",
  "description": "A full-stack web application for Code-a-Thon",
  "main": "index.js",
  "scripts": {
    "start": "cd Backend && npm start",
    "dev": "cd Code-a-Thon && npm run dev",
    "install-all": "npm install && cd Backend && npm install && cd ../Code-a-Thon && npm install",
    "build": "cd Backend && npm install && npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@sendgrid/mail": "^8.1.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
